import React from 'react';
import ReactDOM from 'react-dom';
import App from './App';

const chatApp = document.getElementById('chat-app');
ReactDOM.render(
    <React.Frament>
        <App/>
    </React.Frament>,
    chatApp
);